{
  "name": "mon-bureau-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . --overwrite --platform=win32 --arch=x64 --icon=mon-icone.ico --prune=true --out=dist"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-packager": "^17.1.2"
  }
}